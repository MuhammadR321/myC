#include <stdio.h>
#include <math.h>
int main(void)
{
    int a, b, c, d, e;
    float mean;
    int sum;
   
    sum = (a + b + c + d + e);
    mean = sum / 5;
    scanf("%d %d %d %d %d\n", a, b, c, d, e);
    printf("%0.1f\n", mean);
} 
