#include <stdio.h>
#define SUCCESS 0

int main(void) {
	float mean;
	int a[5]
	int sum;
	int i;

	printf("Enter Integer Values = ");
	scanf("%d %d %d %d %d", &a, &b, &c, &d, &e);
	
	for(i=0; i<5; i++)
		a[i] = i;
	for(i=0; i<5; i++)
		printf(
	sum = a + b + c + d + e;
	mean = sum /(float)n;
	printf("Mean Value = %0.2f\n", mean);

	return(SUCCESS);
}
